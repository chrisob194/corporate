# Plan — #37

Two independent tasks, no dependency between them, both in one wave. T1 authors `plugins/corporate/skills/tailwind-playbook/SKILL.md` — the eighteenth stack playbook — transcribing the pinned fact set F1–F13 from the design into the five fixed body sections, with the current major in `## Stack` and the superseded one in `## Traps`, which is how the release distinction is held without a section the contract forbids. T2 closes the three repo-level enumerations that would otherwise claim seventeen playbooks ship, bumps the plugin version, and adds the one-line cross-reference from the sibling `angular-playbook`. The file sets are disjoint, so neither blocks the other.

## T1 — Author the tailwind-playbook skill
depends_on: none
files: plugins/corporate/skills/tailwind-playbook/SKILL.md
acceptance: `bun run validate` exits 0 with no error naming `tailwind-playbook`; `grep -n '^## ' plugins/corporate/skills/tailwind-playbook/SKILL.md` prints exactly `## Stack`, `## Toolchain`, `## Obligations by activity`, `## Traps`, `## Resources` in that order; `grep -c 'tailwindcss\.com' plugins/corporate/skills/tailwind-playbook/SKILL.md` prints `1` (one line, the single pinned-source obligation row, carrying at most two URLs); `sed -n '/^## Toolchain/,/^## Obligations/p' plugins/corporate/skills/tailwind-playbook/SKILL.md | grep -E '^\| [a-z]' | grep -vc bun` prints `0` (every toolchain command is a bun command)
steps:
  - Read `docs/authoring.md:88-172` — the playbook contract — and `plugins/corporate/skills/angular-playbook/SKILL.md` plus `plugins/corporate/skills/zod-playbook/SKILL.md` as the two nearest models for density and voice. Read `docs/corporate/37/design.md`: its `### Pinned Tailwind facts (F1–F13)` is the authority for every Tailwind claim in this file.
  - Confirm each fact you write at the URL the design names for it (the pages under `tailwindcss.com/docs` cited in F1–F13). Write no Tailwind fact that is not in F1–F13 or confirmed at those URLs; if a fact you believe the file needs is in neither, stop and report it as a design gap rather than writing it from memory.
  - Create the directory and file with frontmatter `name: tailwind-playbook` (must equal the directory) and a one-line `description:` in "Use when …" form naming Tailwind's identifiers, not just its name — include at least `tailwindcss`, `@tailwindcss/vite`, `@tailwindcss/postcss`, `@tailwindcss/cli`, `@import "tailwindcss"`, `@theme`, `@source`, `@utility`, `@apply`, `@reference`, `@config`, `tailwind.config.js`, `@tailwind base`, `postcss.config.mjs`, `.postcssrc.json` — and a closing clause for the case where a project styles with utility classes in markup rather than a stylesheet.
  - `## Stack`: state flat what the stack is. Tailwind CSS v4 is the current major (F1) and the major in a project's own `tailwindcss` dependency governs which rules apply. Then the three wiring paths as a short `| Pipeline | Packages | Plugin registered in |` table from F2 — Vite (`tailwindcss`, `@tailwindcss/vite`, `vite.config.ts`), PostCSS (`tailwindcss`, `@tailwindcss/postcss`, `postcss`, `postcss.config.mjs` — or `.postcssrc.json` where the framework guide says so, F12), no bundler (`tailwindcss`, `@tailwindcss/cli`, none). Then the stylesheet entry point, one line and identical in all three: `@import "tailwindcss";` (F3). Then the governing configuration: that same stylesheet is the config file — `@theme` at top level, the namespaces `--color-*`, `--font-*`, `--text-*`, `--breakpoint-*`, `--spacing-*`, `--radius-*`, `--shadow-*`, `--color-*: initial` to clear one and `--*: initial` to clear all, and that theme variables are also emitted as real CSS custom properties (F4). Then: `tailwind.config.js` still works but is opt-in through `@config`, and `corePlugins`, `safelist` and `separator` do not work at all (F5). Then source detection: no `content` array, plain-text scanning that skips `.gitignore`d files, `node_modules`, binaries, CSS files and lockfiles, extended with `@source`, `source("…")`, `source(none)`, `@source not` and `@source inline()` (F6). Facts only, no procedure for discovering them, and no version-resolution ladder.
  - `## Toolchain`: a `| Job | Command |` table, every command bun — add it to a Vite pipeline (`bun add tailwindcss @tailwindcss/vite`), add it to a PostCSS pipeline (`bun add tailwindcss @tailwindcss/postcss postcss`), add it with no bundler (`bun add tailwindcss @tailwindcss/cli`), build or watch without a bundler (`bunx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch`), check the installed major (`bun pm ls | grep tailwindcss`), migrate a v3 project (`bunx @tailwindcss/upgrade`). Then the absolute ban: never `npm`, `npx`, `yarn` or `pnpm` to reach any of these — not in a shell, not in a `package.json` script, not in CI — including when translating a copied upstream snippet, which prints `npm install` and `npx` throughout. State that the upgrade tool needs Node.js 20 or higher and is run on a fresh branch whose diff is then reviewed (F10).
  - `## Obligations by activity`: a `| Activity | Obligation |` table, agent-agnostic, keyed to activities, with **exactly one `any` row** carrying the pinned doc source and the only URLs in the file — `tailwindcss.com/docs` is the authority, it publishes no `llms.txt` so exact pages are reached through its own navigation, and `tailwindcss.com/docs/installation/framework-guides` is the subtree holding the per-build-tool wiring; a fact either covers is read there, never asserted from memory, and no blog post, no v3-era tutorial and no framework's own README substitutes; where the tree is unreachable the installed `tailwindcss` package's own files are the fallback. Keep this to one row — `docs/corporate/36/review.md:40` records the sibling playbook shipping it as two, and `docs/authoring.md:158-161` says one.
  - Add the remaining obligation rows: **choosing an approach** — name which of the three wiring paths the pipeline dictates and which major is in play, in writing, before any package is added, because the packages, the plugin registration file and the config location all follow from those two answers and the v3 answer is wrong for every path; **implementing** — one stylesheet entry `@import "tailwindcss";`, theme tokens in `@theme` in that same file, extra sources through `@source`, custom utilities through `@utility`, and complete class names only; **migrating** — run the upgrade tool on a fresh branch and read the diff, treating the renamed utilities in `## Traps` as the part the tool cannot verify for you; **reviewing** — reject a `@tailwind` directive, a `tailwind.config.js` relied on without `@config`, a `content` array, an `autoprefixer` or `postcss-import` entry left beside `@tailwindcss/postcss`, a `!`-prefixed important modifier, and any class name assembled from fragments; **testing** — the compiled stylesheet is the artifact, so assert that a class the markup uses actually appears in the output, because source detection and not the markup decides what ships (F6, F7).
  - `## Traps`, from F5–F12: `@tailwind base/components/utilities` for the single `@import "tailwindcss";`; a `tailwind.config.js` sitting in the repo and silently ignored because v4 does not auto-detect it, and `corePlugins`/`safelist`/`separator` not working even when it is loaded; a `content` array, replaced by detection plus `@source`, with safelisting now `@source inline(...)`; `tailwindcss` left as the PostCSS plugin entry when the plugin moved to `@tailwindcss/postcss`; `autoprefixer` or `postcss-import` left in the chain when v4 does both itself; `npx tailwindcss -i … -o …` when the CLI moved to `@tailwindcss/cli`; `@layer utilities` for `@utility`. Then, as the highest-value entry, **the silent renames** — copy these from F9 rather than from memory: `shadow-sm`, `rounded-sm`, `blur-sm`, `drop-shadow-sm` and `backdrop-blur-sm` all exist in v4 and all mean what the unsuffixed v3 class meant, so a surviving v3 class name renders a different size with no error; plus `ring` → `ring-3` and `outline-none` → `outline-hidden`. Then: `!flex` for `flex!`; `bg-opacity-50` and its siblings for the `/50` modifier; Sass, Less or Stylus alongside Tailwind, which v4 is explicitly not designed for and which a workspace scaffolded `--style scss` discovers late; `@apply` inside a component-scoped stylesheet with no `@reference` to the global sheet, since component styles compile separately and know nothing of the theme; a dynamic class name, whose failure is a missing style and not an error; assuming the browser floor is free, when v4 needs Chrome 111, Safari 16.4 and Firefox 128 and older support means staying on v3.4; and `--force` copied from the Angular guide's npm line, which is an npm peer-resolution flag, not a Tailwind package or option, and has no place in a bun command.
  - `## Resources`: `### Skills` listing the bare names `angular-playbook` and `bun-pm-playbook`; `### MCP servers` reading `None.` Names only — no summaries, no versions, no paths, no "use this for X". Do not describe, quote, date or audit any other component's content, and never imply this plugin ships a Tailwind server.
  - Keep the whole file in the 110–150 line band the comparable playbooks occupy, with no code block over ~15 lines, and add no sibling file.
  - Run the acceptance commands.

## T2 — Register the eighteenth playbook and bump the version
depends_on: none
files: README.md, CLAUDE.md, plugins/corporate/.claude-plugin/plugin.json, plugins/corporate/skills/angular-playbook/SKILL.md
acceptance: `bun run validate` exits 0; `grep -riq seventeen README.md CLAUDE.md` exits non-zero (no stale count remains); `grep -q tailwind-playbook README.md && grep -q tailwind-playbook CLAUDE.md && grep -q tailwind-playbook plugins/corporate/skills/angular-playbook/SKILL.md` exits 0; `grep -q '"version": "5.3.0"' plugins/corporate/.claude-plugin/plugin.json` exits 0
steps:
  - In `README.md:263`, change "Seventeen ship today" to "Eighteen ship today", and add one clause to the list in the established form — `tailwind-playbook` for the utility-class styling surface — placed immediately after the `angular-playbook` clause at `README.md:273` so the "one per Bun doc area" sentence stays last.
  - In the `Skill` row of the component inventory table at `README.md:418`, add `tailwind-playbook` to the comma-separated list, after `angular-playbook` and before the Bun four, matching the order the prose list uses.
  - In `CLAUDE.md:37-41`, change "seventeen stack playbook skills" to "eighteen" and add `tailwind-playbook` to the parenthesised list in the same position — after `angular-playbook`, before "and one per Bun doc area".
  - In `plugins/corporate/skills/angular-playbook/SKILL.md:98-102`, add the bare name `tailwind-playbook` to the `### Skills` list. One line, no summary, no path — `docs/authoring.md:144-146` makes `## Resources` names-only. Change nothing else in that file.
  - Bump `plugins/corporate/.claude-plugin/plugin.json` `version` from `5.2.0` to `5.3.0`.
  - Do not create, edit or describe the contents of `plugins/corporate/skills/tailwind-playbook/SKILL.md` — that file belongs to T1 and may not exist yet in this worktree. `docs/ideas.md` holds no Tailwind entry and must not gain one; it is out of scope.
  - Run the acceptance commands.

## Test suites

| Suite | Layer | Command | Setup |
|---|---|---|---|
| plugin structure | integration | `bun run validate` | — |

## Waves

| Wave | Tasks | Runs in parallel |
|---|---|---|
| 1 | T1, T2 | yes — disjoint file sets, no shared path |
